{
  "name": "ideavault-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express + MongoDB API server for IdeaVault — a startup idea sharing platform.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.js",
    "start": "node src/server.js",
    "seed": "node --env-file=.env src/seed.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^7.3.0",
    "morgan": "^1.10.0"
  }
}
